// 一些配置文件
const base: string = 'admin-web';
const appName: string = 'admin-web';
const authToken: string = '';
const authMobile: string = '';

export default {
  base: base,
  appName: appName,
  authToken: authToken,
  authMobile: authMobile
}